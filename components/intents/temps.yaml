call_east_temp_intent:
  speech:
    text: 'The East Wing is {{ states.climate.east_wing.attributes["current_temperature"] }} degrees.'

call_west_temp_intent:
  speech:
    text: 'The West Wing is {{ states.climate.west_wing.attributes["current_temperature"] }} degrees.'

call_house_temp_intent:
  speech:
    text: 'The east wing is {{ states.climate.east_wing.attributes["current_temperature"] }} degrees and west wing is {{ states.climate.west_wing.attributes["current_temperature"] }} degrees.'
